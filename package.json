{
  "name": "claudectl",
  "version": "0.1.0",
  "description": "A TypeScript CLI tool for Claude project management",
  "main": "dist/index.js",
  "bin": {
    "claudectl": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/cli.js",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build",
    "lint": "biome lint src/",
    "format": "biome format --write src/",
    "check": "biome check --write src/",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "cli",
    "claude",
    "typescript",
    "project-management",
    "terminal"
  ],
  "author": "",
  "license": "MIT",
  "files": [
    "dist/**/*"
  ],
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "commander": "^11.0.0",
    "zod": "^4.0.17"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
